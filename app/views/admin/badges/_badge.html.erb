<tr>
   <td><%= image_tag badge.image, class: "badge-image" %></td>
   <td><%= badge.name %></td>
   <td><%= badge.rule.name %></td>
   <td><%= link_to t('show'), [:admin, badge] %></td>
   <td><%= link_to t('edit'), edit_admin_badge_path(badge) %></td>
   <td><%= link_to t('delete'), [:admin, badge], method: :delete, data: { confirm: t('sure?') } %></td>
 </tr> 